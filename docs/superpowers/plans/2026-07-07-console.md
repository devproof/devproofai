# Devproof AI — Sub-plan D: Console (MVP) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:executing-plans. Checkbox steps.

**Goal:** Minimal Next.js console: Catalog page (browse curated models, one-click deploy to a pool) and Deployments page (live phase/endpoint, gateway sync, delete). Dev-mode only (out-of-cluster, talks to control plane on 127.0.0.1:7080 via Next rewrites). Helm packaging of the full platform is deferred to the containerization milestone (recorded deviation from concept §8 — dev-profile Helm chart lands when operator/control-plane/console have images).

**Tech Stack:** Next.js 15 (App Router, TS), React 19. No UI library — hand-rolled minimal styling.

### Task 1: Scaffold + catalog page
**Files:** `console/package.json`, `next.config.ts`, `tsconfig.json`, `app/layout.tsx`, `app/globals.css`, `app/page.tsx` (catalog, server component), `app/actions.tsx` (client buttons)
- [ ] Scaffold, `npm install`, dev server starts
- [ ] Catalog page renders seed entry from `GET /v1/catalog` (via rewrite `/api/*` → control plane)
- [ ] Deploy button POSTs `/api/v1/deployments` (name = catalogId + suffix, poolRef `cpu-default`) then POSTs gateway sync
- [ ] Commit `feat(console): catalog page with one-click deploy`

### Task 2: Deployments page + E2E
**Files:** `console/app/deployments/page.tsx`
- [ ] Lists deployments (name, catalogId, pool, phase, endpoint, replicas) from `GET /v1/deployments`; refresh; Sync gateway + Delete buttons
- [ ] E2E: deploy from catalog UI → deployment appears → reaches Ready → gateway smoke passes for it → delete from UI
- [ ] `node scripts/smoke-serving.mjs` and `smoke-gateway.mjs` green
- [ ] Commit `feat(console): deployments page, e2e verified`
